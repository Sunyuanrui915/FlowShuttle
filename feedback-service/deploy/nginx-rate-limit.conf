# Install under /etc/nginx/conf.d/ so these zones are declared in the http context.
limit_req_zone $binary_remote_addr zone=flow_feedback_submit:10m rate=2r/m;
limit_req_zone $binary_remote_addr zone=flow_feedback_image:10m rate=12r/m;
